# Compatibility review: mods, shader packs, resource packs, servers, loaders

Players run this mod next to dozens of others. The rules, in order:

1. **Never crash** with another mod: no mixin conflict at load, no exception escaping at runtime.
   Any path to a crash is a **REAL BUG**.
2. **Degrade to vanilla**: where the fix can't model what another mod does, the line keeps
   vanilla's value (or vanilla's visibility), not a worse one. A newly introduced worse-than-vanilla
   outcome is a REAL BUG unless the user accepts it as a documented Known limitation.
3. **Don't break the other mod**: chain with it instead of replacing or cancelling what it hooks.
4. **No regressions**: a mod, shader pack or resource pack handled before must still be handled. A
   Known limitation that changed without `CLAUDE.md` changing is a **WRONG FACT**.

## A. Mixin mechanics (every injector the scope adds or changes)

- **Chain, don't replace.** Use MixinExtras injectors that compose with other mods
  (`@ModifyExpressionValue`, `@ModifyReturnValue`, `@WrapOperation`, `@WrapWithCondition`) and
  non-cancelling `@Inject`. No `@Redirect` (only one mod can redirect a call; the next one fails),
  no `@Overwrite`, no cancelling vanilla work other mods hook (the old cancel-and-redraw of the line
  dropped other mods' line tweaks).
- **Contested targets.** For each target, find other mods that hook the same method. A `@Redirect`
  or `@Overwrite` there can remove our injection point, and with `require ≥ 1` that is a startup
  crash for everyone with both mods. Keep required hooks to what the fix can't work without, on the
  narrowest target; everything else `require = 0` with a vanilla fallback.
- **Cancels upstream.** Another mod cancelling at a method's HEAD skips everything after it. Place
  hooks where that is seen and harmless (the hand-pass mark sits past such cancels by design).
- **Names.** Everything added to a vanilla class is `@Unique` and prefixed `fishingrodfix$` —
  fields, methods, interface methods, `@Accessor`/`@Invoker` methods.
- **Priority.** The default, unless a documented conflict requires otherwise.
- **Mod-id special cases.** An `isModLoaded` check uses the exact id from that mod's
  `fabric.mod.json`, is cached in a `static final`, and documents what it assumes about the mod's
  configuration (e.g. Clearviews' default "Disable Nausea").
- **Calling hooked vanilla methods.** Invoking vanilla methods that other mods inject into (the
  view-bob and hurt-tilt invokers) runs their injections once more per frame. A stateful injection
  (smoothing over time, counters, side effects) would misbehave: check the known bob and camera
  mods' injections into those methods.
- **Read, don't re-derive.** Reading the value vanilla already computed (the extracted projection,
  camera, hand state) follows other mods' changes automatically; recomputing it from options or
  player fields silently ignores them. A re-derived value is a compatibility smell.
- **No dependencies.** No Fabric API or libraries; MixinExtras comes with the loader floor.

## B. What other mods can change, and what the fix reads

Use this to spot interactions the matrix doesn't list. The fix reads:

| Input | Examples of mods that change it |
|---|---|
| which branch of the hand-position method vanilla takes | full-body first person, third-person camera mods |
| the camera: entity, position, rotation (incl. roll), detached, panoramic | freecam, replay, camera roll/tilt, portals, VR |
| the world projection (world FOV) | zoom, FOV mods, orthographic cameras, screenshot mods |
| the hand FOV | hand-FOV options in viewmodel mods, shader packs |
| the hand pass's extracted state (items, equip heights, swing, sway inputs, use state) | animation libraries, swap-animation mods |
| the hand pass's pose applied in the renderer, not in the state | viewmodel mods, first-person animation mods |
| the body's render state and model pose (where the branch corrects the third-person origin) | player animation mods, model mods, scale mods |
| view bob and hurt tilt, on both passes or one | bobbing and shake mods |
| the nausea / portal warp in the world pass | anti-nausea mods, clients, Iris |
| whether and when the hand pass submits a hand; frames that extract without rendering | hand-hiding mods, stereo/screenshot mods |
| the HUD-hidden flag and the rest of the hand gate | F1 mods, photo mods |
| the rod's sprite, model and display transform | resource packs, CIT, server item models |
| the item class of the held and drawn items (`FishingRodItem`) | modded rods, server custom items |
| how many times a hook is extracted per frame; its owner | shader shadow passes, portal views, replay |
| the line's geometry submission (`lines` render type) | line-rendering mods, Polytone |

## C. Known ecosystem (observed — verify against the source, extend it)

Status: **H** handled by design (see where), **L** documented Known limitation in `CLAUDE.md`,
**U** unverified (check it when the change touches what it changes). A row is only as good as the
branch it was checked on: the mod may not exist for this version, or may have changed.

### Renderers and performance mods

| Mod | What it changes that matters | Status |
|---|---|---|
| Sodium | replaces terrain rendering; entities still go through vanilla's renderers | U: check that hook extraction and `submit` still run through `FishingHookRenderer`. 26.1.2 (`26.1.2/stable`, checked 2026-09-22): no mixins on `FishingHookRenderer`, `ItemInHand*` or `SubmitNodeStorage`; its `GameRendererMixin` only copies `renderLevel`'s projection. 1.21.11 (0.8.14/0.8.15, checked 2026-09-22): no mixins on the port's targets (its frapi `SubmitNodeCollection.clear` is another class) 1.21.10 (0.7.2-0.7.3, branch `1.21.9`, checked 2026-09-22): H: no mixins on `FishingBobberEntityRenderer`, `HeldItem(Feature)Renderer` or `OrderedRenderCommandQueueImpl`; its `GameRenderer` mixins are a fog accessor and a GUI inject in `render`; its frapi `SubmitNodeStorage` mixin only adds `fabric_submitItem`. 1.21.8 (mc1.21.8-0.7.x, branch `1.21.8/stable`, checked 2026-09-22): H: no mixin on `FishingBobberEntityRenderer`, `HeldItem(Feature)Renderer`, `WorldRenderer.renderEntities`, `VertexConsumerProvider$Immediate` or the entity sort. 1.21.5 (mc1.21.5-0.6.13-fabric, branch `1.21.5/stable`, checked 2026-09-23): H: no mixin on `FishingBobberEntityRenderer`, `HeldItem(Feature)Renderer`, `WorldRenderer.renderEntities`, `getEntitiesToRender` or `VertexConsumerProvider$Immediate`; its `LevelRendererMixin` `@Overwrite`s only terrain/dirty-section/block-entity methods; its `GameRendererMixin` injects at `render`'s `GuiGraphics.flush()`, not at `isFinishedLoading`; its `SpriteContents` mixins only rewrite the RGB of alpha-0 pixels, so the alpha-only tip measurement is unaffected. 1.21.4 (`mc1.21.4-0.6.13-fabric`, branch `1.21.4/stable`, checked 2026-09-23): H: no mixin on `FishingBobberEntityRenderer`, `HeldItem(Feature)Renderer`, `WorldRenderer.renderEntities`/`getEntitiesToRender`/`renderEntity`, `VertexConsumerProvider$Immediate`, `Camera` or `GameOptions`; its `GameRendererMixin` injects at `render`'s `GuiGraphics.flush()`, not at `isFinishedLoading`; its mipmaps `SpriteContents` `@WrapOperation` rewrites only the RGB of alpha-0 pixels (alpha untouched, so the tip measurement is unaffected) and its own `SpriteContentsAccessor` targets `byMipLevel`, not `image`. |
| Iris (+ any shader pack) | draws the hand with its own hand renderer; a shadow pass extracts hooks a second time, into its own `LevelRenderState` and `SubmitNodeStorage`, and keeps that frame's render states until its next shadow frame (also after a disconnect: the pipeline survives it); wraps the nausea warp | H: its hand renderer reaches the hand-pass mark; frames are counted per frame, not per call (`HandPass`), and the first-person origin is worked out once per frame and reused by the shadow extraction (`FishingLineOrigin`); a body-held hook's render state drops its owner at `submit` (`ThirdPersonLineOrigin`); its shadow pass's `ShadowRenderer.extractVisibleEntities` skips only spectators, so with entity shadows it draws the local body with its rod, then the hook, also in first person: a first-person line left at vanilla's value with the camera on the player takes only a same-pass rod, so the shadow's line sits on the shadow body's rod and the visible one keeps vanilla's value (Iris 26.3 branch, checked 2026-09-22; its 26.2 and 26.1 branches skip only spectators too, checked 2026-09-22). L: Clearviews + Iris' warp wrapper when Iris wins the load order: `MixinModelViewBobbing` (not `MixinGameRenderer`) `@WrapOperation`s `renderLevel`'s warp `Matrix4f.rotate`/`scale`, the calls Clearviews wraps, and with a pack in use moves bob and warp into the model view without calling the original (Iris 26.2 and 26.3 branches, checked 2026-09-22). 26.2 (1.11.4, observed 2026-09-22): `HandRenderer` calls `ItemInHandRenderer.submitHandsWithItems` (the mark fires); the shadow pass runs at `LevelRenderer.render`'s `addMainPass` call, before Iris' hand pass (`renderSolid` inside the main pass, `renderTranslucent` at the end of `render`), so the origin cached at the main extraction still holds there (on 26.2 it reads the live player and renderer, which change only in ticks); pipelines are kept per dimension and destroyed only on a dimension change or shader reload (`Iris.getCurrentDimension()` returns the last dimension while no level is loaded). 26.1 (1.11.4+26.1, branch `26.1` bff1e69, checked 2026-09-22): `HandRenderer` → `iris$renderHandsWithCustomRenderer` → `renderHandsWithItems` (the mark fires); with a pack, vanilla's `renderItemInHand` call is `@Redirect`ed to a no-op and the hand's closing `renderAllFeatures` is replaced by `HandRenderer.endRender()`, which clears Iris' own storage inside the main pass after `submitEntities`; the shadow pass runs at `renderLevel`'s `addMainPass` INVOKE with its own `LevelRenderState`, `SubmitNodeStorage` and `FeatureRenderDispatcher`, cleared by its own `renderAllFeatures` before the main pass (so 26.1.x's global clear count still separates it); `MixinModelViewBobbing` as on 26.2, so the Clearviews L holds. 1.21.11 (1.10.7+1.21.11, checked 2026-09-22): `HandRenderer` reaches the 5-arg `HeldItemRenderer.renderItem` (the mark fires) and draws the hand at `getFov(false)`; the shadow pass runs at `WorldRenderer.render`'s `setupFrustum` INVOKE (shift after), before vanilla's `fillEntityRenderStates`, so it is the frame's first extraction (the per-frame origin is worked out there, from the same state), into its own `OrderedRenderCommandQueueImpl`/`RenderDispatcher`, whose clears `OrderedRenderCommandQueueImplMixin` counts; its entity extraction skips only spectators (`shadowEntities` defaults to true), so it draws the local body and rod, then the hook, also in first person: the same-pass rule puts the shadow's line on the shadow body's rod and keeps the visible one at vanilla's value; with a pack, bob and warp move into the model view (`@Redirect`s of `bobView`/`tiltViewWhenHurt`, of the warp intensity's `Double.floatValue`, returning 0 so vanilla's warp is skipped, and of `Matrix4f.rotation`, re-applying it): same product, but out of Clearviews' reach (see Clearviews) 1.21.10 (1.9.7+1.21.10, branch `1.21.9` 11e691d, checked 2026-09-22): H: `HandRenderer.renderSolid`/`renderTranslucent` call the 5-arg `renderHandsWithItems` (the mark fires, its own `renderArmWithItem` HEAD cancel sits past it) and draw at `getFov(false)`; its queue is its own and cleared by its own `renderAllFeatures` (counted); the shadow pass runs at `renderLevel`'s `prepareCullFrustum` (shift after), i.e. after `Camera.update` and the world `getFov` but before `fillEntityRenderStates`, and extracts every entity passing `shouldRender`, camera entity included, before it submits any; its `@ModifyArgs` on `render`'s `GlobalSettingsUniform.update` coexists with the frame counter at the same call's `GlobalSettings.set`. L: `MixinModelViewBobbing` redirects the warp intensity's `Double.floatValue` (0 with a pack) and `Matrix4f.rotation` and re-applies bob and warp in the model view, out of Clearviews' reach. 1.21.8 (1.9.x, branch `1.21.6` = 1.21.6/1.21.7/1.21.8, checked 2026-09-22): H: `HandRenderer` calls the 5-arg hand pass and draws at `getFov(false)`; the shadow pass uses its own `RenderBuffers`, so its collector identity separates it; `batchedentityrendering/MixinLevelRenderer` (priority 999) injects only at `renderEntities`' `renderEntity` INVOKE (start/endGroup, no flush) and `MixinLevelRenderer_EntityListSorting` (999) wraps `collectVisibleEntities`' iterator *before* vanilla's stable `ENTITY_COMPARATOR` sort, so it can't change the player-vs-bobber group order; `MixinFishingHookRenderer` is a non-cancelling `@Inject` after the line loop. `FullyBufferedMultiBufferSource` overrides `endBatch()`, so the `renderEntities` mark is what ends the pass with a pack. L: `MixinModelViewBobbing` moves bob and warp into the model view (same product, out of Clearviews' reach). 1.21.8 draw order (checked 2026-09-23): `MixinLevelRenderer_EntityListSorting` (999) `@WrapOperation`s `collectVisibleEntities`' iterator and regroups by `EntityType`, but that runs before vanilla's own `ENTITY_COMPARATOR` sort and before the port's `renderEntities` HEAD partition, which therefore has the last word; a contiguous bobber run moved to the end preserves every other type's grouping, so Iris' batching premise holds. Its `ShadowRenderer` never calls `WorldRenderer.renderEntities` — it builds and sorts its own list with `Comparator.comparingInt(e -> e.getType().hashCode())` — so the shadow pass keeps the identity-hash order and its own line is on the shadow body's rod only in the launches where that order puts the body first. Its `MixinRenderBuffers.bufferSource` HEAD swaps in `FullyBufferedMultiBufferSource` whenever level rendering has begun (gated on `begins != 0`, i.e. also without a shader pack), so with Iris installed entity submission order costs nothing in draw calls. Its `batchedentityrendering/MixinFishingHookRenderer` is a `require = 1` `@Inject` at `FishingBobberEntityRenderer.render`'s `MatrixStack.pop()` ordinal 1 — the port's `@WrapWithCondition` on `renderFishingLine` leaves both `pop()`s in place, so it still applies. 1.21.5 (1.8.11+1.21.5-fabric, branch `1.21.5`, checked 2026-09-23): H at load — `MixinGameRenderer` `@Redirect`s only `renderItemInHand`'s inner `renderHandsWithItems` call, so vanilla's `renderHand` still runs and the hand-FOV capture on its `getFov` still fires; `HandRenderer.renderSolid/renderTranslucent` call `renderHandsWithItems` themselves (the hand mark fires) at `invokeGetFov(camera, tickDelta, false)`; `MixinItemInHandRenderer` HEAD-cancels `renderArmWithItem`, past the caller-side mark; `MixinFishingHookRenderer` is a `require = 1` `@Inject` at `render`'s `PoseStack.popPose()` ordinal 1, which the `@WrapWithCondition` on `renderFishingLine` leaves in place; the shadow pass has its own `RenderBuffers` (collector identity separates it), skips only spectators and sorts its own list with `Comparator.comparingInt(e -> e.getType().hashCode())`; `MixinRenderBuffers.bufferSource` swaps in `FullyBufferedMultiBufferSource` whenever `begins != 0` (also with no pack), so the `renderEntities` TAIL mark is what ends a pass; `MixinModelViewBobbing` moves bob and warp into the model view with a pack (same product; the Clearviews L holds). **The draw order is Iris', not vanilla's:** `batchedentityrendering/mixin/MixinLevelRenderer_EntityListSorting` (priority 999, in the always-loaded `iris-batched-entity-rendering.mixins.json`, not gated on a pack being in use) `@WrapOperation`s `getEntitiesToRender`'s `Iterable.iterator()` and regroups the entities by `EntityType` through a `HashMap`, an identity-hash order fixed per launch. On 1.21.6–1.21.8 vanilla's own `ENTITY_COMPARATOR` ran after it; on 1.21.5 (and on 1.21.9+/26.x) nothing does, so **a port that relies on the client's add order without forcing it is a REAL BUG with Iris installed** — the 1.21.5 port keeps the bobbers-last partition at `renderEntities`' head for exactly this reason (found in review, 2026-09-23). 1.21.4 (1.8.8+1.21.4-fabric, branch `1.21.4`, checked 2026-09-23): H at load. `MixinGameRenderer` `@Redirect`s only `renderItemInHand`'s inner `renderHandsWithItems` call, so vanilla's `renderHand` and the hand-FOV capture on its `getFov` still run; `pathways/HandRenderer` calls `renderHandsWithItems` at `invokeGetFov(camera, tickDelta, false)` (the hand mark fires). `ShadowRenderer` builds its own `RenderBuffers` (collector identity separates it), runs at `renderLevel`'s `collectVisibleEntities` INVOKE (shift AFTER), i.e. before `renderMain`/`renderEntities`, iterates `entitiesForRendering()` itself, skips only spectators and sorts by `EntityType.hashCode()`. `MixinRenderBuffers.bufferSource` HEAD-returns `FullyBufferedMultiBufferSource` whenever `begins != 0` (also with no pack) and its `endBatch()` never calls super, so the `renderEntities` TAIL mark is what ends a pass. `batchedentityrendering/MixinLevelRenderer` (999) injects only at `renderEntities`' `renderEntity` INVOKE; `MixinLevelRenderer_EntityListSorting` (999, in the always-loaded `iris-batched-entity-rendering.mixins.json`) `@WrapOperation`s `collectVisibleEntities`' iterator and regroups by `EntityType` through a `HashMap`, and vanilla 1.21.4 sorts nothing afterwards, so the port's priority-1500 `renderEntities` HEAD partition has the last word and is **required**. `batchedentityrendering/MixinFishingHookRenderer` is a `require = 1` `@Inject` at `render`'s `popPose()` ordinal 1 doing `getBuffer(RenderType.lineStrip()).addVertex(0,0,0).setColor(0,0,0,255).setNormal(0,0,0)`: the port's `@WrapWithCondition` removes no instruction, so both `pop()`s and Iris' point survive, and the three `@ModifyVariable` STORE ordinals are unaffected (Iris' point is after the three `fstore`s at ofs 110/120/130, and Mixin resolves every injection point in `PREINJECT` before applying any). When the port **hides** a line that single vertex is duplicated by `BufferBuilder.endVertex` (LINES/LINE_STRIP) into a two-vertex degenerate strip; `VertexFormats.LINES`' required elements are all written, so nothing throws, and `setNormal(0,0,0)` makes the lines shader's position NaN, so nothing is drawn. In Iris' own `SegmentedBufferBuilder` one `BufferBuilder` serves the whole pass per `RenderType` and `splitStrip()` is triangle-strip-only, so that vertex is the strip separator and removing a hook's 17 vertices changes nothing visible. L: `MixinModelViewBobbing` with a pack `@Redirect`s `renderLevel`'s `bobHurt`/`bobView` and the warp intensity's `Double.floatValue()` (0), then re-applies bob, warp and the camera rotation to the model view — same product, out of Clearviews' reach. **Per-entity buffer sources (checked 2026-09-23, Iris branches `1.20.1`, `1.21.1`, `1.21.4`, `1.21.5`, `1.21.6`): `entity_render_context/MixinEntityRenderDispatcher` is a `@ModifyVariable(argsOnly = true, allow = 1, require = 1)` on `EntityRenderDispatcher.render`'s `MultiBufferSource` argument that returns `new BufferSourceWrapper(...)` for every entity as soon as `WorldRenderingSettings.INSTANCE.getEntityIds() != null` and `ImmediateState.isRenderingLevel`. `setEntityIds` is called only from `IrisRenderingPipeline`'s constructor and nothing ever resets it (`VanillaRenderingPipeline` resets the block ids, not the entity ids), so once any pack has been loaded it holds until the game restarts. A rod (drawn from the player renderer through that dispatcher) and its hook (likewise) therefore never share a collector, which used to make every pass match fail and left MC-4490 / MC-176514 / MC-198777 / MC-247425 / MC-270173 / MC-270174 unfixed for every shader user. The pass tag now also carries “inside the world's entity loop”, recorded by `WorldRendererMixin`'s head hook, and two drawings of one loop match whatever their collectors are: H. The shadow pass stays separate because Iris renders it from a `renderLevel` inject that runs before the entity loop (`collectVisibleEntities` on 1.21.4+, `renderSky` on 1.20.1 and 1.21.1) and its `ShadowRenderer` never calls the vanilla entity loop, so its drawings see the previous frame's record; with a pack its own rod-to-hook pairing still fails on the per-entity wrappers, as before, so a first-person line left at vanilla's value stays vanilla there instead of taking the shadow body's rod.** **Shadow pass, verified 2026-09-23 on branch `1.21.6` (the same shape on `1.21.5`, `1.21.4`, `1.21.1`, `1.20.1`): the per-entity wrapper covers it too, because `ImmediateState.isRenderingLevel` is set for the whole of `renderLevel` (`iris/mixin/vertices/immediate/MixinLevelRenderer`, priority 999) and `pipeline.renderShadows` is injected inside it; `ShadowRenderer` has its own private `renderEntities` calling `invokeRenderEntity` per entity, so neither of the port's two hooks on the world's entity loop ever fires for it. A shadow pass therefore exists only where every entity has its own collector, and its own rod and hook never pair — in any launch order. Also: with Iris installed at all, `RenderBuffers.bufferSource()` returns `FullyBufferedMultiBufferSource` for the whole level render (`begins != 0` from `renderLevel` HEAD, pack or not), whose `endBatch()` override bypasses `VertexConsumerProviderImmediateMixin`, so with Iris present the entity-loop end mark is the only pass end inside the world — which is what keeps the GUI's pictures apart.** |
| Sodium Extra, Reese's Sodium Options | option toggles | U: check whether any toggle touches view bob, FOV or the hand |
| ImmediatelyFast | batches immediate-mode rendering | U: the line is a deferred custom-geometry submit. 26.1 (checked 2026-09-22): `FeatureRenderDispatcher` injects only call `endLastBatch`; nothing touches `clear` 1.21.10 (1.13.6, branch `1.21.9`, checked 2026-09-22): H: its `RenderDispatcher.render` injects only call `drawCurrentLayer`; it doesn't hook `clear()`. **1.21.8 (1.12.6+1.21.8, branch `discontinued/1.21.8`, checked 2026-09-22): it `@Overwrite`s the static `VertexConsumerProvider.immediate(SequencedMap, BufferAllocator)` factory to return its own `BatchableBufferSource extends Immediate`, whose `draw()` never calls super (universal batching is on by default), and `BufferBuilderStorage` builds both of vanilla's sources through it — so a pass tag that only counts `Immediate.draw()` stays 0 for the session. That is why 1.21.6–1.21.8 also mark `WorldRenderer.renderEntities`' return (`WorldRendererMixin`). It has no mixin on `renderEntities`, so that mark is safe: H.** 1.21.8 draw order (checked 2026-09-23): `BatchableBufferSource` keeps one `BufferBuilder` per layer for the whole batch and `drawCurrentLayer()` sorts `activeLayers` by `getLayerOrder` (stable), so entity submission order changes no draw-call count — the port's bobbers-last partition is free there. 1.21.5 (1.9.7+1.21.5-fabric, branch `discontinued/1.21.5`, checked 2026-09-23): H — still `@Overwrite`s the static `VertexConsumerProvider.immediate(SequencedMap, BufferAllocator)` → `BatchableBufferSource`, whose `draw()` never calls super, so the `Immediate.draw()` count stays 0 and the `renderEntities` TAIL mark carries the pass tag. No `renderEntities` mixin; its `GameRenderer` hooks are a `getEntityVertexConsumers()` redirect and an inject after `DrawContext.draw()`. 1.21.4 (1.8.7+1.21.4-fabric, tag v1.8.7, checked 2026-09-23): H: still `@Overwrite`s the static `VertexConsumerProvider.immediate(SequencedMap, BufferAllocator)` → `BatchableBufferSource`, whose `draw()` never calls super (universal batching on by default), so the `Immediate.draw()` count stays 0 and the `renderEntities` TAIL mark carries the pass tag. No `WorldRenderer` mixin; its `GameRenderer` hooks are a `getEntityVertexConsumers()` redirect and an inject after `DrawContext.draw()`. Its `getBuffer` creates a fresh `BufferBuilder` for every shared-vertex layer request, so line strips never merge and a hidden line's empty request is a no-op. |
| Anything hooking `SubmitNodeStorage.clear` / `FeatureRenderDispatcher.clearSubmitNodes` (26.1.x) | 26.1.x' pass tag counts the storage's clears (`SubmitNodeStorageMixin`, MEV on `clear()`'s only `submitsPerOrder` read) | H: no known mod hooks it (checked 2026-09-22: the mods in this matrix); the count stops only if `clear()` is overwritten or cancelled at HEAD, and then a rod drawn after its hook just isn't remembered. 1.21.11: the same count through `OrderedRenderCommandQueueImpl.clear()` (`OrderedRenderCommandQueueImplMixin`, MEV on its only `batchingQueues` read, `require = 0`); no mod checked on 1.21.11 hooks it (checked 2026-09-22: Polytone's mixin targets `submitModel`, frapi's another class) 1.21.10: the same count through `OrderedRenderCommandQueueImpl.clear()`; Sodium, Iris, ImmediatelyFast 1.13.6, Polytone 3.6.3 and SkyHanni don't hook it (checked 2026-09-22). 1.21.6–1.21.8: no deferred queue — a pass ends at `WorldRenderer.renderEntities`' return (`WorldRendererMixin`, `@Inject` TAIL, priority 1500, `require = 0`) or at any `VertexConsumerProvider$Immediate.draw()` (`VertexConsumerProviderImmediateMixin`, MEV on its only `layerBuffers` read, `require = 0`). Subclasses bypass the second without any mixin: ImmediatelyFast's `BatchableBufferSource`, Iris' `FullyBufferedMultiBufferSource` and its `UnflushableWrapper`, Real Camera's `MultiVertexCatcher.MeshCatcher` — hence the first mark. No mod checked 2026-09-22 mixes into `draw()V` or into `renderEntities` other than by non-cancelling `@Inject`s (Freecam, Real Camera, First Person Model, Iris). 1.21.6–1.21.8: `WorldRendererMixin` (mixin priority 1500) holds two optional `@Inject`s on `WorldRenderer.renderEntities` — TAIL ends a pass, HEAD stable-partitions the passed list so every `FishingBobberEntity` is drawn after every other entity. Checked 2026-09-23: the only mods touching that method on these versions are Iris 1.9.7 (`@Inject` at the `renderEntity` INVOKE, priority 999), First Person Model 2.7.2 (`@Inject` HEAD, 1000), Freecam 1.3.4+mc1.21.6 (`@Inject` TAIL, `locals = CAPTURE_FAILHARD`, 1000) and Real Camera 0.7.4-beta-1.21.8 (`@Inject` RETURN, 1000) — all non-cancelling, none reads or reorders the list. Freecam's local capture is computed in `INJECT_APPLY` at its own priority, i.e. before the priority-1500 injections are inserted, and a non-cancellable single `@Inject` adds no LVT entry, so the capture is safe. |
| Entity Culling, MoreCulling | skip entities that aren't visible | U: a culled hook isn't extracted, like vanilla; the visibility decision rides on the render state 1.21.8 (Entity Culling 1.11.2, MoreCulling 1.4.0-beta.2, checked 2026-09-23): H — Entity Culling cancels per-entity `WorldRenderer.renderEntity` at HEAD (order-independent; a culled owner just leaves the remembered spot), and its `renderEntities` RETURN inject is commented out for `>=1.21.4 && <1.21.11`; MoreCulling only `@Accessor`s `cullingFrustum`. 1.21.5: Entity Culling 1.11.2 covers `>=1.21.4 && <1.21.11` with the same per-entity `renderEntity` HEAD cancel (order-independent; a culled owner leaves the remembered spot), checked 2026-09-23. 1.21.4 (Entity Culling 1.11.2, checked 2026-09-23): H: only `@Inject(renderEntity, HEAD, cancellable)` (order-independent; a culled owner leaves the remembered spot); the `renderEntities` RETURN inject in the `>=1.21.4 && <1.21.11` arm is line-commented out and not compiled. |
| Lithium, FerriteCore, ModernFix, C2ME, BadOptimizations | logic, memory, loading, render caches | U: low risk; check if the change touches sprites/atlases (ModernFix) or cached render state (BadOptimizations) 1.21.8 (checked 2026-09-23): Lithium `mc1.21.8-0.18.1` has no render package and no `WorldRenderer` mixin; ModernFix has no 1.21.6–1.21.8 build. |
| Dynamic FPS | throttles or skips frames while unfocused | U: check the frame-counting assumptions (a frame extracted without its hand pass). 1.21.11 (3.11.6, branch `1.21.11`, checked 2026-09-22): an MEV on `GameRenderer.render`'s `skipGameRender` read skips most of its 15 loop iterations a second while it throttles (unfocused: 1 fps target, idle after 5 min: 10 fps); H: the port counts frames at `render`'s `updateCamera` call, past that check (a counter at `render` HEAD counted the skipped iterations as frames without a hand: vanilla's line). 26.x: whether its skip leaves `extract` (the counter) running is unchecked 1.21.10 (3.11.6, branch `1.21.9` 9632dd5, checked 2026-09-22): H: an MEV on `render`'s `skipGameRender` read skips the whole block, and the counter is that block's first call, so skipped iterations aren't counted. Its `renderLevel` HEAD cancel (the level covered by an overlay) leaves a counted frame with no samples and no line. 1.21.5 (3.11.4, branch `1.21.5`, checked 2026-09-23): H — `GameRendererMixin` MEVs `render`'s `Minecraft.noRender` (= `skipGameRender`) read and the skipped frame returns *before* the `isFinishedLoading` call the 1.21.5 port counts frames at, so throttled frames aren't counted as hand-less. Its `renderLevel`/`renderItemActivationAnimation` HEAD cancel leaves a counted frame with no `renderWorld` sample — gate closed, latch restored, nothing drawn. 1.21.4 (3.11.8+minecraft-1.21.2-fabric, branch `1.21.2`, checked 2026-09-23): H: an MEV on `GameRenderer.render`'s `Minecraft.noRender` (= `skipGameRender`) GETFIELD; vanilla's `isFinishedLoading()` sits inside `if (!skipGameRender)`, so a throttled frame returns before the frame counter. Its `renderLevel` HEAD cancel leaves a counted frame with no `renderWorld` sample — gate closed, latch restored, nothing drawn. |
| Distant Horizons | extra LOD terrain passes | U: check that its passes don't extract entities 1.21.8 (3.3.2-1.21.8, checked 2026-09-23): its `MixinLevelRenderer` hooks only `prepareChunkRenders` HEAD for `>=1.21.6 && <1.21.9`; terrain/LOD only, no entity extraction. 1.21.4 (3.3.2-1.21.4, GitLab main, checked 2026-09-23): H: `MixinLevelRenderer` compiles one `renderSectionLayer` HEAD inject (terrain/LOD); `MixinGameRenderer` is an empty mixin retargeted onto `Entity` for `MC_VER <= 1.21.11`. Nothing on entities. |
| VulkanMod | replaces the render backend | U |
| Fishing Ruler (`fishing_ruler`) | client-side fishing-distance HUD | H (1.21.4, checked 2026-09-23): its client mixin list is `ClientWorldMixin`, `FishingBobberEntityMixin`, `FishingRodItemMixin` — no renderer mixin. Its `@Unique` state on `FishingBobberEntity` coexists with the port's `FishingBobberEntityMixin`. |

### Camera, FOV, view

| Mod | What it changes that matters | Status |
|---|---|---|
| Zoom mods (Just Zoom, Ok Zoomer, Zoomify, …) | narrow the world FOV | H by design: the world FOV is read from the extracted projection. U per mod: check it zooms through the camera or projection and leaves the hand FOV and pose alone. 1.21.11: the world FOV is `renderWorld`'s own `getFov` result (captured by an MEV on its call; no projection is read), so mods that zoom inside `getFov` (MRV / RETURN / TAIL / HEAD cancel) are followed (checked 2026-09-22: Ok Zoomer 16.0.0-beta.2, Logical Zoom 0.0.34, WI Zoom 1.7, Just Zoom 2.1.3, Zume 1.2.2, Vanilla Zoom, Custom FOV 12.0.0, Camera Utils 1.1.2, Tweakeroo's zoom); mods that change the projection itself aren't. L: Zoomify 2.15.2 with "Affect Hand FOV" off (not its default) zooms the hand back with an MEV on `renderWorld`'s second `getFov` call: its order against the capture is settled by the capture's mixin priority of 900 against Zoomify's default 1000 (an expression modifier is inserted right after the instruction and mixins apply in ascending priority, so the first-applied ends up outermost and this mod reads the value Zoomify leaves; checked 2026-09-23), the invoker fallback during zoom changes doesn't see it, and with an Iris pack the hand is drawn at `getFov(false)` anyway (the line is off while zoomed, as vanilla's is). Ok Boomer 0.1.5 advances its transition on every `getFov` call, so the invoker fallback adds a step during its transitions (vanilla's locator bar does the same) 1.21.10 (checked 2026-09-22): Zoomify 2.15.2+1.21.9 (`getFov` MRV, followed; its `bobView` MEV applies to both passes and the invoker) — L with "Affect Hand FOV" off (not its default): an MEV on `renderWorld`'s ordinal-1 `getFov` multiplies the zoom back, as on 1.21.11; Ok Zoomer 15.0.0-beta.5 (`getFov` MRV at RETURN ordinal 1 + `bobView` MEV): H; Tweakeroo 0.26.5 (`getFov` HEAD cancel, priority 1001): H. 1.21.5 (Zoomify 2.15.2+1.21.5, checked 2026-09-23): H — `getFov` MRV, followed by both captures and by the invoker fallback. L with "Affect Hand FOV" off (not its default): on `<1.21.6` its `keepHandFov` is an MEV on **`renderItemInHand`'s only `getFov`** — the same instruction the 1.21.5 port's hand-FOV capture uses. Both are MEVs, so they chain and there is no load conflict; the order between them is undefined, which is the same outcome class as the 1.21.6+ L. Its `secondary` mixin MEVs `render`'s `Options.hideGui` (the GUI) only, which the live sampling in `renderWorld` doesn't see. 1.21.4 (Zoomify 2.15.2+1.21.5, whose jar declares 1.21.3/1.21.4/1.21.5; checked 2026-09-23): H — zoom is a `@ModifyReturnValue` on `getFov`, followed by both captures and by the invoker fallback; its `bobView` MEV scales the amplitude at the `strideDistance` field read, i.e. both passes and the invoker alike; its secondary mixin MEVs only `render`'s `Options.hideGui` read, which neither `renderHand`'s gate nor the port's live `hudHidden` sample sees. L with “Affect Hand FOV” off (not its default): `keepHandFov` is an MEV on `renderItemInHand`'s only `getFov` call — the very instruction the port's hand-FOV capture uses. Both are MEVs, so no load conflict, but the order between them is undefined. |
| Better F1 Reborn | keeps the hand with the HUD hidden | H: normal path. 1.21.11 (1.1, checked 2026-09-22): flips `hudHidden` at `renderWorld` HEAD and TAIL; either order stays corrected 1.21.10 (1.1, `fabric1.21.x` 275381a, checked 2026-09-22): H: in NO_HUD mode it sets `hudHidden` false at `renderWorld` HEAD and true at TAIL, and the port samples inside `renderWorld` after `Camera.update`, so it always sees the HUD shown (the normal path). 1.21.5 (1.1, branch `fabric1.21.x`, checked 2026-09-23): H — it sets `hudHidden` false at `renderWorld` HEAD and true at TAIL, and the port samples inside `renderWorld` after `Camera.update`, so it always sees the HUD shown. 1.21.4 (BetterF1-Fabric-1.1-1.21.4, checked 2026-09-23): H — it writes the real `options.hudHidden` false at `renderWorld` HEAD and true at TAIL; the port samples inside `renderWorld` after `Camera.update`, so it always sees the HUD shown and takes the normal path. |
| Custom FOV (`customfov`), Camera Utils (`camerautils`) | change the FOV inside `getFov` | H: the captured `getFov` result follows them (1.21.11, checked 2026-09-22) 1.21.10: Custom FOV has no 1.21.9-1.21.10 build (checked 2026-09-22). |
| OrthoCamera (`orthocamera`) | orthographic world projection | L: on 1.21.11 there is no projection to read, so the line keeps a perspective placement (0.1.10, checked 2026-09-22) |
| Freecam mods | camera on another entity, or detached / away from the eye | H: the first-person origin keeps vanilla's value (`FishingLineOrigin`, `HandPass`). Freecam (MinecraftFreecam; main branch, 26.3 build 1.5.0-alpha.1; checked 2026-09-22) keeps the camera type first person, makes its own `FreeCamera` the camera entity and with Show Player (default on) adds the local player at `LevelExtractor.extractVisibleEntities` RETURN (on 26.1.x at `LevelRenderer.extractVisibleEntities` RETURN: the same main-branch source builds 1.5.0-alpha.1+mc26.2 and +mc26.1.2), after the hooks: H, the line takes the body's rod from the remembered spot, one frame late (`ThirdPersonLineOrigin`; the same-pass-only rule applies only with the camera on the player). L: with Show Player off, a remembered spot (an F5 view, Iris' shadow pass) can still place the line where the hidden body's rod was. 1.21.11 (Freecam 1.4.1, checked 2026-09-22): first person forced, its own `FreeCamera` as camera entity, Show Player on by default, the player appended at `WorldRenderer.fillEntityRenderStates` RETURN (after the hooks: the remembered spot); with Show Player off its `EntityRenderManager.shouldRender` mixin also keeps the player out of Iris' shadow extraction, so only an F5 view can leave a remembered spot 1.21.10 (Freecam 1.3.5+mc1.21.10, checked 2026-09-22): H: its own `FreeCamera` is the camera entity; Show Player (default on) appends the player at `fillEntityRenderStates` RETURN, after the hooks, so the line takes the remembered spot, refreshed every frame by Iris' shadow pass. L: with Show Player off its `shouldRender` cancel covers only its own `FreeCamera` and its Iris mixin cancels `renderPlayerEntity`, which Iris 1.9.7 never calls, so the shadow pass still draws the hidden body and the line starts at that rod every frame with entity shadows. 1.21.5 (Freecam 1.3.3+mc1.21.5, branch `1.21.5`, checked 2026-09-23): H — its own `FreeCamera` is the camera entity, so the first-person origin keeps vanilla's value; Show Player (default on) draws the body at `renderEntities` TAIL at the default injector priority, before the port's priority-1500 TAIL mark, so it is inside the pass and the line takes the remembered spot one frame late. Its `CAPTURE_FAILHARD` there captures only the method's own arguments and the port's two non-cancellable single-point injects allocate no LVT slot. Its `ItemInHandRenderer` `@ModifyVariable(HEAD, argsOnly)` player swap doesn't touch the caller-side hand mark. 1.21.4 (Freecam 1.3.2+mc1.21.4 — **the only 1.21.4 build**; tag v1.3.2+mc1.21.4, checked 2026-09-23): H — its own `FreeCamera` is the camera entity and the camera type is forced first person and never detached, so the first-person origin keeps vanilla's value and `samePassOnly` is **not** set. Show Player (default on) draws the body at `renderEntities` TAIL with `locals = CAPTURE_FAILHARD` at default priority, before the port's priority-1500 TAIL mark, so it is inside the pass but after the hooks: the remembered spot, one frame late. The capture is safe (the port's two injects there are single-point and non-cancellable, so no LVT slot is allocated). The hand is hidden through vanilla's `GameRenderer.setRenderHand(false)`, not a mixin. |
| First Person Model, Real Camera | send the local player into the hand-position method's third-person branch while they draw its body (First Person Model adds its body offset at the method's return) | H: the first-person hook doesn't see them. Where the branch also corrects the third-person origin, check that it follows the body they draw. Real Camera (26.3/dev source; no 26.3 build yet, 26.2 is 0.7.8-beta; checked 2026-09-22): `@WrapOperation` on `getPlayerHandPos`' `CameraType.isFirstPerson` while `RealCameraCore.isRendering()`; at `LevelRenderer.submitEntities` RETURN its classic mode submits the camera entity into the level collector (after the hooks: the remembered spot), its binding mode into its own `RoutingSubmitCollector` (L: the line starts at the spot remembered from another pass: Iris' shadow pass every frame with entity shadows, else an earlier F5 view; vanilla's value until then). 26.1 (checked 2026-09-22): First Person Model 2.7.2 (`main`): `@Redirect` of `getCameraType` and a RETURN offset in `getPlayerHandPos`, `submit` HEAD, `submitArmWithItem` HEAD cancel while items are hidden, `LevelRenderer.extractVisibleEntities` HEAD adds the body first (H: its rod matches before the hook). Real Camera 0.7.8-beta (`26.1/dev`): `isFirstPerson` wrap, `renderHandsWithItems` HEAD cancel; classic mode submits the body into the level storage at `submitEntities` RETURN, after the hooks (remembered, a frame late), binding mode through its own `RoutingSubmitCollector` (the L above: the spot remembered from another pass). With the inventory open (26.1.x' shared storage) the GUI model's rod falls after a clear and isn't remembered. 1.21.11 (checked 2026-09-22): First Person Model 2.7.2 H; Real Camera has no 1.21.11 build (0.7.4-beta for 1.21.8; its binding mode submits through its own `RoutingSubmitCollector`) 1.21.10 (checked 2026-09-22): First Person Model 2.7.2 (`main`): H: a `@Redirect` of `getHandPos`' `getPerspective` plus a RETURN offset, `render` HEAD, a `HeldItemFeatureRenderer.renderItem` HEAD cancel while both arms are hidden, a `renderFirstPersonItem` HEAD cancel (the caller-side mark is unaffected), and `fillEntityRenderStates` HEAD adds the body first (same-pass read). Real Camera has no 1.21.9-1.21.10 build (0.7.4-beta for 1.21.8). 1.21.8 (checked 2026-09-22): First Person Model 2.7.2 (`main`, the `>=1.21.3 && <1.21.9` arm): H — it `@Redirect`s the camera-type check in the hand-position method (never `Vec3d.add`), and its `WorldRendererMixin` `@Inject(renderEntities, HEAD)` draws the body into the same `Immediate` before the sorted loop, so it is always in the entity pass (which is why 1.21.6–1.21.8 mark only that method's TAIL). Real Camera 0.7.4-beta-1.21.8 (branch `1.21.8`): H at load (`@Redirect` of `isFirstPerson()`, not `Vec3d.add`); classic mode draws the body at `renderEntities`' RETURN at default priority — before our priority-1500 mark, so it is in the pass and the remembered spot is refreshed; binding mode (the default) renders into its own `MultiVertexCatcher`: the documented L. 1.21.6–1.21.8 (checked 2026-09-23): only that method's TAIL is marked as a pass end, so First Person Model's body, drawn at its HEAD, belongs to the pass that follows; the port's second `@Inject` at that HEAD only reorders the list and draws nothing. 1.21.5 (checked 2026-09-23): First Person Model 2.7.2 (`main`, the `>=1.21.3 && <1.21.9` arm): H — it `@Redirect`s `getPlayerHandPos`' `Options.getCameraType()` (never `Vec3d.add`, so the `allow = 1` MEV is safe) and adds its body offset at that method's RETURN; its `WorldRendererMixin` `@Inject(renderEntities, HEAD)` at default priority draws the body before the port's priority-1500 head walk, so its rod is read on the previous frame's owner set and stays inside the entity pass. **Real Camera has no 1.21.5 build** (Modrinth jumps 1.21.1 → 1.21.6–1.21.8). 1.21.4 (checked 2026-09-23): First Person Model 2.7.2 (the `>=1.21.3 && <1.21.9` arm): H — `@Redirect`s `getHandPos`' `Options.getCameraType()` (never `Vec3d.add`) and adds its body offset at that method's RETURN; its `WorldRendererMixin` `@Inject(renderEntities, HEAD)` at default priority draws the body into the level's own buffer source before every entity and before the port's priority-1500 head walk, so its rod is read on the previous frame's owner key and stays inside the entity pass; its `ItemInHandLayer` HEAD cancel only fires when `hideArmsAndItems()`, false with the default `vanillaHandsMode = OFF`. **Real Camera 0.6.2-beta-1.21.4 does exist** (`xTracr/RealCamera` tag `0.6.2-beta.1.21.4`): H — no fishing-renderer mixin at all on this version and no camera-type change (later versions `@WrapOperation` `getPlayerHandPos`' `isFirstPerson`); `MixinItemInHandRenderer` HEAD-cancels `renderHandsWithItems` while rendering, so there is no hand pass and, with the camera still focused on the player, the first-person line keeps vanilla's value and `samePassOnly` **is** set. `MixinLevelRenderer` `@Inject(renderEntities, RETURN)` at default priority: classic mode draws the body into the level buffer source after the hooks (remembered spot, one frame late, inside the pass); binding mode (the default) replays a private `VertexRecorder` filled at `renderLevel`'s `Camera.setup` INVOKE — a different collector — so with `samePassOnly` that line keeps vanilla's value. Defaults: `enabled = false`, `isClassic = false`. |
| Animatium | rewrites the eye height inside the hand-position method; legacy item positions; "Minimal View Bobbing" | H: vanilla's vector isn't reused. L: legacy item positions; its equip animation `@ModifyArg`s the main-hand `submitArmWithItem` call's stack with its own copy (26.3/development, 4.4.1; checked 2026-09-22), which isn't followed (the extracted `mainHandItem` is read); on 26.1–26.2 the same `@ModifyArg` sits on `renderHandsWithItems`' `renderArmWithItem` (26.1) / `submitHandsWithItems`' `submitArmWithItem` (26.2) main-hand call (26.1/development and 26.2/development, checked 2026-09-22), and the renderer's `mainHandItem` field is read. L: Minimal View Bobbing drops `renderLevel`'s `bobView` only (world-only bob; observed on 26.2). 26.1 (4.4.1, `26.1/development`, checked 2026-09-22): `getPlayerHandPos` `@ModifyArgs`/`@WrapOperation`/MEVs, `@ModifyArg`s in `extractRenderState` and `lambda$submit$1`, no `Vec3.add` redirect; Minimal View Bobbing and legacy positions not rechecked. 1.21.11 (4.4.1, `1.21.11/development`, checked 2026-09-22): L: in its V1_7/V1_8 equip modes, and through a precedence bug whenever Animatium is globally disabled, a `@ModifyArg` hands the main-hand `renderFirstPersonItem` call its own stack (index 5), which isn't followed (the renderer's `mainHand` field is read); its `updateHeldItems` changes drive the equip-progress fields (followed). L: its 1.7/1.8 fishing-rod version moves the first-person rod at `Transformation.apply` (1.7: turned 180° about a pivot) and shifts vanilla's line (+0.15 on `getPosition`'s arguments), which the correction replaces with one on the unmoved rod; third-person injects at default priority (ours, 1500, runs after) 1.21.10 (3.2, `1.21.10/development` e31d68c, checked 2026-09-22): in `getHandPos` it `@ModifyArgs` `getPointOnPlane` (+0.15 in its 1.7/1.8 modes, which the correction replaces), wraps `getCameraPosVec`/`getArmHoldingRod`, MEVs `isInSneakingPose` and the `0.8` constant and `@ModifyArg`s `updateRenderState`'s swing argument, but never redirects `Vec3d.add`: no load conflict. L: first-person `itemPositions` and `fishingRodVersion = V1_7` are on by default (legacy positions; the 1.7 rod is turned 180° before `renderItem`, and while fishing it is a model swap to `minecraft:stick`). L: its main-hand stack `@ModifyArg` under "Fix Equip Animation Item Check" (default on); its `updateHeldItems` wraps are followed. Third person it injects at default priority (ours, 1500, runs after). Its `bobHurt`/`bobView` wraps and `fixVerticalBobbingTilt` are stateless, so the mod's own calls match; L: Minimal View Bobbing (off by default); its `GlobalSettings.set` glint `@ModifyArg` coexists with the frame counter. 1.21.8 (2.5-1.21.8-lunarfix, branch `1.21.8/development`, checked 2026-09-22): H at load — six injectors on the hand-position method, none on `Vec3d.add`; its `MixinItemInHandLayer` (priority 1000) injects at the same `ItemRenderState.render` call, so ours (1500) runs after. New L: it can cancel a sleeping player's body render and force the cast rod model on or off, so the drawn sprite may not be `item/fishing_rod_cast`. 1.21.5 (2.5, branch `1.21.5/development`, checked 2026-09-23): H at load — six injectors on `getPlayerHandPos` plus a `@ModifyArg` in `extractRenderState` and a line-width `@Inject` in `render`, none on `Vec3.add`. `MixinItemInHandLayer` injects at the same `ItemStackRenderState.render` call at priority 1000 (the port's 1500 runs after). `MixinItemInHandRenderer` (priority 500) `@ModifyArg`s the main-hand stack (L) and drives the equip fields through `tick` (followed). `MixinSpriteContents` only wraps `isTransparent`'s `ARGB.alpha`, which `RodSprite` doesn't use. `MixinFishingRodCast` MRVs the cast-rod model property (L). 1.21.4 (2.1; source read on branch `1.21.4/development`, head 2.2, checked 2026-09-23): H at load — its `MixinFishingHookRenderer` has `@ModifyArgs`/`@WrapOperation`s/MEVs and an `@Inject` in `render` at `PoseStack.last()` ordinal 1 (after the three float stores): no `@Redirect` anywhere and nothing on `Vec3.add`. `MixinItemInHandLayer` (priority 1000) injects at the `ItemStackRenderState.render` call — the port's 1500 runs after all of it. `MixinItemInHandRenderer` (priority 500) `@ModifyArgs` the main-hand stack (“Fix Equip Animation Item Check”, default on) and drives the equip fields through `tick` (followed). `MixinLivingEntityRenderer` HEAD-cancels the local sleeping player's body when `modelWhilstSleeping` is off. `MixinSpriteContents` wraps only `isTransparent`'s alpha, not `image`. `MixinGameRenderer` hooks inside `bobHurt`/`bobView` statelessly (the port's invoker calls stay in agreement). New L: `screen/MixinCamera` writes `eyeHeight`/`eyeHeightOld` at `Camera.setup` and calls `move(...)` at its TAIL (legacy parallax), which can push the camera past the port's 0.5-block eye check and close the F1 gate (magnitude unmeasured). |
| Shoulder Surfing Reloaded, Better Third Person | third-person cameras | U: where the branch corrects the third-person origin, check it with their camera and body; line hiding applies in every perspective. 1.21.11 (checked 2026-09-22): Shoulder Surfing Reloaded 5.0.10 takes the body-held path; Better Third Person has no 1.21.11 build 1.21.10 (Shoulder Surfing Reloaded 4.17.1, branch `1.21.10`, checked 2026-09-22): H: the body-held path; its `getFov` `@ModifyVariable` and `renderLevel` bob/crosshair redirects don't touch the calls the mod makes, and its `submit` HEAD cancel for a too-close camera entity just leaves the remembered spot. Better Third Person has no 1.21.9-1.21.10 build. 1.21.5 (Shoulder Surfing Reloaded 1.21.5-4.13.1+fabric, branch `1.21.5`, checked 2026-09-23): H, the body-held path — a `getFov` `@ModifyVariable` (followed) and a `renderEntity` HEAD cancel for a too-close camera entity (leaves the remembered spot). Nothing on `renderEntities`, `getEntitiesToRender` or the fishing renderer. 1.21.4 (Shoulder Surfing Reloaded 1.21.4-4.12.0+fabric, checked 2026-09-23): H, the body-held path — no fishing, `renderEntities` or `getEntitiesToRender` mixin; `@Inject(renderEntity, HEAD, cancellable)` for a too-close, looking-up or scoped camera entity leaves the remembered spot; a `getFov` `@ModifyVariable` (followed); its `Camera` mixins offset the position and add roll after `setPosition`, never touching `cameraY`/`lastCameraY` and never detaching. Better Third Person 1.9.0 not inspected. |
| Do a Barrel Roll, Camera Overhaul | camera roll and tilt | U: if only the world pass gets it, same class as bobbing only one pass (a candidate L). 1.21.11 (checked 2026-09-22): Do a Barrel Roll 3.8.3, Camera Overhaul 2.1.1: H 1.21.10 (Camera Overhaul 2.1.1, `dev`, checked 2026-09-22): H: its state advances once per frame at `Camera.setup` RETURN and its `tiltViewWhenHurt` HEAD inject applies the stored roll statelessly, in both passes and in the mod's invoker call. Do a Barrel Roll 3.8.3 not rechecked (Codeberg blocks fetches). |
| CameraTweaks (first-person freelook), SmoothCamera (F1 glide), Immersive Portals | re-pose the camera or hand | L. Immersive Portals has no 1.21.11 build (checked 2026-09-22) Immersive Portals has no 1.21.6–1.21.8 build either; the repo was archived 2026-04-21, newest branch 1.21.3 (checked 2026-09-23). 1.21.4 (checked 2026-09-23): CameraTweaks (branch `1.21.4`): H at load — `RendererMixin` `@Inject(bobView, HEAD, cancellable)` (stateless; it cancels both vanilla passes and the port's invoker alike), `@ModifyReturnValue(getFov, RETURN)` (followed), `@WrapOperation` on `getFov`'s `SimpleOption.getValue()` ordinal 0 (the options FOV inside the `changingFov` branch — absorbed by the port's shared-FOV factor) and on `renderWorld`'s `SimpleOption.getValue()` ordinal 2, which on 1.21.4 is the frustum `getFov().getValue()`, **not** the nausea scale; `CameraMixin` `@Inject(update, HEAD, cancellable)` plus a `setRotation` redirect (first-person freelook: the documented L); `HeldItemRendererMixin` is a non-cancelling HEAD inject on the 5-arg `renderItem`. Immersive Portals has no 1.21.4 build (repo archived, newest branch 1.21.3). SmoothCamera 1.0.0 lists 1.21.1–1.21.11 on Modrinth but its source declares `minecraft ~1.21.11` / `fabricloader >=26.1` and its `Camera.setup` handler takes the 1.21.9+ `Level` first parameter, so it very likely cannot load or apply on 1.21.4 (NEEDS JAR). |
| Camerapture, Snapmatica, Picture Mode | hide the hand around F1 | L. 1.21.11 (checked 2026-09-22): Camerapture 1.10.12, Snapmatica, Picture Mode keep vanilla's value as documented 1.21.10 (checked 2026-09-22): Picture Mode has no 1.21.9-1.21.10 build. 1.21.4 (checked 2026-09-23): Camerapture 1.10.1+mc1.21.4 (branch `old/1.21.4`): L as documented — its `HeldItemRendererMixin` HEAD-cancels the 5-arg `renderItem` whenever an active camera is held (before the port's mark), the HUD is cancelled at `InGameHud.render` and `options.hudHidden` is flipped true for one capture frame by plain code, restored at `GameRenderer.render` TAIL. Raising the camera with the HUD shown teaches the latch (vanilla's value); F1 first and then the camera keeps the latch and leaves the line at the hidden rod's tip. Snapmatica 1.3.3+1.21.4: its `HeldItemRendererMixin` body is gated to `>=1.21.10`, so on 1.21.4 it hides the hand by setting the private `GameRenderer.renderHand` field false just before `render`'s `renderWorld` call (after the port's frame counter) and restoring it after; it never touches `hudHidden`. Its `@Inject(getFov, RETURN, cancellable)` focal-length override is followed; its `@Inject(getBasicProjectionMatrix, RETURN, cancellable)` aperture shear is **not** (new L); its `@Inject(bobView, HEAD, cancellable)` during its freecam cancels both passes and the port's invoker alike; its `CameraMixin` freecam keeps the player as `focusedEntity` and forces `thirdPerson` true by default, so vanilla draws the body and the line lands on its rod. Picture Mode has no 1.21.4 build. |
| Replay Mod, Flashback | camera on another entity in replays; offscreen export renders | U: expect vanilla's value in replays; exports multiply passes (performance) |
| Vivecraft | VR: the rod follows a tracked controller and it has its own line origin | U, high risk: check its changes to `FishingHookRenderer` against our required injector. 26.2 (1.3.15, observed 2026-09-22): `getPlayerHandPos` HEAD cancel in VR passes plus a `Mth.lerp` wrap, `Vec3.add` untouched: no load conflict; the VR body path stays U. 26.1.2 (1.3.15, `Multiloader-26.1`, checked 2026-09-22): as 26.2. 1.21.11 (1.3.15, checked 2026-09-22): no load conflict; VR HEAD-cancels `getHandPos` for its own player; each eye is its own `GameRenderer.render` call, counted as a frame (no reuse across eyes); the VR body path stays U 1.21.10 (1.3.15, `Multiloader-1.21.9`, checked 2026-09-22): no load conflict (a VR `getHandPos` HEAD cancel and a `Mth.lerp` wrap; non-VR behaves like vanilla); its `ItemInHandLayer` inject sits after `setArmAngle` and is followed. The VR body path stays U. 1.21.5 (1.3.15, branch `Multiloader-1.21.5`, checked 2026-09-23): no load conflict — a `getPlayerHandPos` HEAD cancel plus a `Mth.lerp` wrap (no `Vec3.add`), `getFov`/`bobHurt`/`bobView` HEAD cancels (the mod's invoker calls get the same treatment), an MEV on `renderLevel`'s `renderHand` field (hand pass suppressed → vanilla's value) and a `render` cancel at `Minecraft.getWindow()` ordinal 2, i.e. after the frame counter. `LevelRendererVRMixin` (990) hooks `renderEntity` and `collectVisibleEntities` but reorders nothing. The VR body path stays U. 1.21.4 (1.21.4-1.3.15-fabric, branch `Multiloader-1.21.4`, checked 2026-09-23): H at load — its `FishingHookRendererVRMixin` is a `@WrapOperation` on `getHandPos`' `MathHelper.lerp` ordinal 0 plus an `@Inject(getHandPos, HEAD, cancellable)`; no `Vec3d.add` injector, so no conflict with `allow = 1`. The VR body path stays U. |
| Tweakeroo | world-only view-bob toggle; freecam; "Disable Offhand Rendering" | L: the toggle. U: its freecam (26.2: cancels the hand pass → vanilla's value). L: Disable Offhand Rendering cancels the off hand's `submitArmWithItem` (as Hide Hands; observed on 26.2). 26.1 (0.28.10, sakura-ryoko `LTS/26.1`, checked 2026-09-22): the off hand's `renderArmWithItem` HEAD cancel (L); its `tick` `getItemSwapScale` wrap is followed through the renderer fields. 1.21.11 (0.27.15, checked 2026-09-22): its freecam uses its own camera entity (vanilla's value); its zoom is a `getFov` HEAD cancel at priority 990, followed; its view-bob wrap is off with Iris 1.21.10 (0.26.5, sakura-ryoko `LTS/1.21.10`, checked 2026-09-22): H zoom (`getFov` HEAD cancel at priority 1001); L the world-only view-bob toggle (a `@Redirect` of `renderWorld`'s `bobView`) and Disable Offhand Rendering (a `renderFirstPersonItem` HEAD cancel); H its freecam: it uses its own camera entity but `@Redirect`s `fillEntityRenderStates`' fourth `getFocusedEntity`, so the player is extracted in order and the rod is read in the same pass. 1.21.5 (0.24.3, sakura-ryoko `LTS/1.21.5`, checked 2026-09-23): H zoom (`getFov` HEAD cancel at priority 1001) and H freecam (its own camera plus a `getEntitiesToRender` `getFocusedEntity` redirect, so the player is still extracted in list order). Its freecam also HEAD-cancels `renderHand` (priority 1005) — the optional hand-FOV capture just doesn't run and `getFov` is asked instead — and the 5-arg `renderItem`. L: the world-only view-bob `@Redirect` of `renderWorld`'s `bobView` (999) and Disable Offhand Rendering (`renderFirstPersonItem` HEAD cancel). 1.21.4 (0.23.6, `sakura-ryoko/tweakeroo` branch `LTS/1.21.4`, config `mixinPriority 990` / `defaultRequire 0`, checked 2026-09-23): H zoom (`@Inject(getFov, HEAD, cancellable)` at priority 1001, so both FOVs agree). **H freecam** (corrects the older “line off the rod” reading): its own `CameraEntity extends ClientPlayerEntity` via `mc.setCameraEntity` — so the first-person correction bails at its camera check and the FOV changes never reach it — a `renderHand` HEAD cancel and both `renderItem` HEAD cancels at priority 1005, and a `@Redirect` of `getEntitiesToRender`'s `Camera.getFocusedEntity()` **ordinal 3** (1.21.4: the `!(entity instanceof ClientPlayerEntity) \|\| …` guard), so the local body **is** drawn in the level pass, in list order, and the line sits on its rod. L: the world-only view-bob `@Redirect` of `renderWorld`'s `bobView` (priority 999) and Disable Offhand Rendering (`renderFirstPersonItem` HEAD cancel for OFF_HAND, past the caller-side mark). |
| BetterHandBobbing, View Bobbing Options, No Screen Bobbing, ShakeTweaks | bob one pass differently from the other | L |
| Clearviews 2.x (`clearviews`) | removes the nausea warp from the world pass | H: the warp isn't undone (assumes "Disable Nausea" on, its default). L: with it off. 26.2 (2.1.7, observed 2026-09-22): `@WrapOperation` on `renderLevel`'s warp `Matrix4f.rotate`/`scale`, `disableNausea` defaults to true; no 26.3 build yet (2026-09-22). 26.1.2 (2.1.7+26.1.2, branch `clearviews-fabric-26.1.2-custom-config`, checked 2026-09-22): the same wraps (`rotate` ordinals 0/1, `scale`), `disableNausea` defaults to true. 1.21.11 (2.0.6, commit 3168863, checked 2026-09-22): wraps `renderWorld`'s warp; L, whatever the load order: with an Iris pack in use Iris applies the warp itself in the model view (see Iris), out of Clearviews' reach, so the world stays warped while the mod skips undoing it 1.21.10 (2.0.6+1.21.10, commit 3168863, checked 2026-09-22): H with "Disable Nausea" on (`@WrapOperation` on `renderWorld`'s warp `Matrix4f.rotate` ordinals 0/1 and `scale`); L with an Iris pack in use, which applies the warp in the model view where Clearviews can't remove it. 1.21.4 (2.0.6+1.21.4, `michiel1106/clearviews-multiloader` branch `mainfr`, checked 2026-09-23): H with “Disable Nausea” on — mod id exactly `clearviews`; three `@WrapOperation`s on `renderWorld`'s `Matrix4f.rotate(F,Vector3fc)` ordinals 0/1 and `Matrix4f.scale(FFF)`, verified to be the only such calls in 1.21.4's `renderWorld` and to be exactly the warp block; `disableNausea` defaults to true; its `LevelRendererMixin` is an empty class body. L with it off, and L with an Iris pack in use. |
| Clearview 1.x (`clearview`) | removes the nausea effect client-side | H: nothing to special-case |
| Anti-nausea in Meteor, Wurst, LiquidBounce | drop the warp without Clearviews' id | L |
| Pixelshot | large screenshots flag only the render state; its orthographic view replaces only the world pass's copy of the projection | H: a large screenshot behaves like F1. Not handled: the ortho view isn't seen (noted in `FishingLineOrigin`, not in `CLAUDE.md`) |
| sbsshot | extracts a frame without rendering it | L. no 1.21.11 build (checked 2026-09-22) 1.21.10: no build (checked 2026-09-22). |

### Hand, viewmodel, animation

| Mod | What it changes that matters | Status |
|---|---|---|
| Punchy, ViewModel, ScaleMe, Smallhands, Inspect Animations | re-pose the hand in the renderer without touching its state | L. Punchy's 26.2 build (2.8a, closed source) is unchecked: check it doesn't redirect `getPlayerHandPos`' `Vec3.add` (our required injector). 1.21.11 (checked 2026-09-22): ViewModel 1.10.8 `@Overwrite`s `renderFirstPersonItem` (the caller-side mark is unaffected), ScaleMe 3.3.1, Inspect Animations 0.2.3: L; Hold My Items 5.1.1, Punchy 2.8a, Fancy View Model, dyn: closed source, unchecked (likely the same L) 1.21.10 (checked 2026-09-22): not rechecked per mod; Hold My Items 5.1.1, VulkanMod 0.6.6, EMF 3.3.8 and Do a Barrel Roll 3.8.3 were not inspected on this version (NEEDS JAR). 1.21.4 (checked 2026-09-23): of these only ViewModel ships for 1.21.4; Punchy, ScaleMe and Inspect Animations have no 1.21.4 build and Smallhands has no Modrinth project, so on this version the class is carried by ViewModel Changer (`viewmodel`), Hold My Items and Fancy View Model (the last two closed source — NEEDS JAR: check for a `@Redirect`/`@Overwrite` on `getHandPos`, which would break the required `allow = 1` MEV). |
| Mods that change `ItemInHandRenderer.submitHandsWithItems`' (26.2; `renderHandsWithItems` on 26.1.x) locals or arguments, or `evaluateWhichHandsToRender` (26.1–26.2) | swing, sway, equip dip or hand selection as drawn | L: on these versions the hand pass reads the live player and its renderer's fields and the hand selection is mirrored, so changes there aren't followed; changes to the renderer's fields (Player Animation Library's and Animatium's `tick` hooks) are |
| ShieldDisruptor, InteractiveStuff (26.2) | cancel `renderItem` for configured items; scripted re-posing | L: the item-only hide and re-pose classes |
| Player Animation Library (Emotecraft, Better Combat, …) | cancels the hand pass; hides only the item during a first-person transition | H: no hand pass → vanilla; its first-person model mode fakes `Camera.isDetached` in `LevelExtractor.extractVisibleEntities` (main branch, 26.3), so the body is drawn earlier in the level pass and the line sits on its rod (same-pass read). L: the item-only hide. 26.1 (1.2.6, branch `26.1`, checked 2026-09-22): first-person model mode by an MEV on `Camera.isDetached` in `LevelRenderer.extractVisibleEntities` (the body is extracted with the other entities, before the hooks: same-pass read); `renderHandsWithItems` HEAD cancel; item-only cancel in `renderItem`; `ItemInHandLayer` injects at default priority (ours, 1500, runs after). 1.21.11 (1.1.10, checked 2026-09-22): feature-renderer injects at default priority (ours, 1500, runs after); equip changes at `updateHeldItems` TAIL, read through the renderer's fields; first-person model mode: same-pass read 1.21.10 (1.1.3, branch `1.21.9`, checked 2026-09-22): H: its `HeldItemFeatureRenderer` injects (the first `mulPose` and `ItemRenderState.render`) sit at default priority, so ours (1500) runs after; its first-person model mode fakes `Camera.isDetached` in `fillEntityRenderStates` (same-pass read) and cancels `renderHandsWithItems` at HEAD; it has no `updateHeldItems` hook and its `getHandRenderType` redirect is commented out. L: its item-only cancel in `renderItem`. 1.21.8 (branch `1.21.6/7`, checked 2026-09-22): H: its `HeldItemMixin` `@Inject`s at the same `ItemRenderState.render` call at default priority, so ours (1500) runs after; its first-person mixins cancel the hand pass and the item-only `renderItem` overload (L); first-person model mode MEVs the camera's detached check, so the body joins the type-sorted entity list. Emotecraft 3.0.0-b.139 (branch `1.21.8`) delegates all posing to it and touches none of the port's targets. 1.21.8 (branch `1.21.6/7`, checked 2026-09-23): its first-person model mode is a `@ModifyExpressionValue` on `Camera.isDetached()` inside `collectVisibleEntities`, so vanilla itself adds the local player to the entity list; with the port's bobbers-last partition that body is now always drawn before the hook, so the same-pass read fires in every launch. Its `FirstPersonMode.setFirstPersonPass` window (raised in `collectVisibleEntities`, cleared at `renderEntity` TAIL for the camera entity) now always closes before the bobbers; its only consumer is `EntityRenderDispatcherMixin.renderShadow`, guarded by `IPlayerAnimationState && isLocalPlayer()`, which a hook's state fails — a no-op. 1.21.5 (2.0.2+1.21.5, branch `1.21.5`, checked 2026-09-23): H — `HeldItemMixin` `@Inject`s at the same `ItemStackRenderState.render` call at default priority, so the port's 1500 runs after; first-person model mode is an MEV on `Camera.isDetached()` inside `getEntitiesToRender`, so vanilla itself adds the local body to the draw list, ahead of the hooks once the port's partition has run. L: its `renderHandsWithItems` HEAD cancel, its `evaluateWhichHandsToRender` redirect (the mirrored hand selection doesn't see it) and its item-only `renderItem` cancel. 1.21.4 (playerAnimator 2.0.5+1.21.4-fabric, branch `1.21.4`, checked 2026-09-23): H — `HeldItemMixin` `@Inject`s at the same `ItemStackRenderState.render` call at default priority, so the port's 1500 runs after; first-person model mode is an MEV on `Camera.isDetached()` inside `getEntitiesToRender`, so vanilla itself adds the local body to the draw list, ahead of the hooks once the port's partition has run. Its only raised priority (2000) is a `LivingEntityRenderer.layers` `@Redirect` that filters the layer list down to `PlayerItemInHandLayer`, so the rod still renders. L: its `renderHandsWithItems` HEAD cancel in `THIRD_PERSON_MODEL` and its item-only `renderItem` cancel. |
| Hide Hands, Exposure | cancel one arm's `submitArmWithItem` (`renderArmWithItem` on 26.1.x) | L. Hide Hands 26.1.2-4.6 unchecked (only a 26.2 source branch). 1.21.11 (4.6, checked 2026-09-22): a priority-1001 HEAD cancel of `renderFirstPersonItem` through Collective: L; Exposure has no 1.21.11 build 1.21.10 (Hide Hands 4.6 with Collective `1.21.10`, checked 2026-09-22): L: a priority-1001 HEAD cancel of `renderFirstPersonItem`. Exposure has no 1.21.9-1.21.10 build. 1.21.8 (checked 2026-09-22): Hide Hands 4.5 ships an empty mixin config — the cancel is Collective's `@Mixin(value = ItemInHandRenderer.class, priority = 1001)` `@Inject(renderArmWithItem, HEAD, cancellable)` (L). Exposure has no 1.21.6–1.21.8 build. 1.21.4 (Hide Hands 1.21.4-4.5 with Collective 1.21.4-8.3, checked 2026-09-23): L — Hide Hands itself has no renderer mixin; the cancel is Collective's `@Mixin(value = ItemInHandRenderer.class, priority = 1001)` `@Inject(renderArmWithItem, HEAD, cancellable)`, per hand, past the port's caller-side mark. Exposure has no 1.21.4 build. |
| Not Enough Animations | mostly third-person animations | U: check its first-person options. 1.21.11 (1.12.6, checked 2026-09-22): L (its off-hand hiding) 1.21.10 (1.12.6, checked 2026-09-22): L: a `HeldItemFeatureRenderer.renderItem` HEAD cancel (its off-hand hiding). 1.21.8 (1.12.x, the `>=1.21.4 && <1.21.9` variant, checked 2026-09-22): L — an `ItemInHandLayer.renderArmWithItem` HEAD cancel (its off-hand hiding). 1.21.4 (1.12.5/1.12.6, `tr7zw/NotEnoughAnimations` main, the `>=1.21.4 && <1.21.9` arm, checked 2026-09-23): L — `ItemInHandLayerMixin` `@Inject(renderArmWithItem, HEAD, cancellable)` at default priority; `HeldItemHandler.onRenderItem` cancels for off-hand hiding and for `isSleeping() && dontHoldItemsInBed` (both arms). A HEAD cancel skips the port's inject at the inner call whatever its priority. |
| Mine Little Pony (first-person unicorn magic) | its `LevitatingItemRenderer` wraps `renderItem`'s `ItemRenderState.render` and moves the first-person rod | L: the line stays on the vanilla rod's position, vanilla's is off too (4.15.5+1.21.11, `fpsmagic` on by default, checked 2026-09-22) 1.21.10 (4.15.2+1.21.10, checked 2026-09-22): H: its `getHandPos` HEAD cancel returns null for the local player in first person, so our hook still runs, and its `HeldItemFeature` extends `PlayerHeldItemFeatureRenderer`, so a pony's rod is read. L: its magic-glint pass submits into a separate queue (third-person magic) and its `LevitatingItemRenderer` wraps the 6-arg `renderItem`'s `ItemRenderState.render` (first-person magic). 1.21.8 (4.14.4+1.21.7, covers 1.21.7–1.21.8, checked 2026-09-22): H third person — no fishing-renderer mixin, and its `HeldItemFeature` calls the inherited vanilla `renderItem`, so a pony's rod is read with the pony pose. L with third-person magic: that same `render` calls `renderItem` **twice more** with a glint-less state through its own wrapped provider, and the reading is last-write-wins, so a glow copy is what gets stored and the line falls back to the remembered or vanilla value. 1.21.5 (4.14.2+1.21.5, branch `1.21.5`, checked 2026-09-23): **no fishing-renderer mixin at all on this version**, so nothing contests `getHandPos`; its `HeldItemFeature extends PlayerHeldItemFeatureRenderer` and calls the inherited `renderItem`, so a pony's rod is read with the pony pose. Its `MixinGameRenderer` injects at `renderWorld` HEAD/RETURN and, in an inner `@Mixin(WorldRenderer.class, priority = 0)`, at `render`'s `drawCurrentLayer()` — not at `renderEntities`. L stays for first-person unicorn magic and for the third-person magic glow copies (last-write-wins). 1.21.4 (4.14.2+1.21.4, branch `1.21.4`, checked 2026-09-23): **no fishing-renderer mixin at all** on this version, so nothing contests `getHandPos`; its `HeldItemFeature extends PlayerHeldItemFeatureRenderer` and calls the inherited `renderItem`, so a pony's rod is read with the pony pose. L: first-person unicorn magic (`MixinHeldItemRenderer` `@WrapOperation`s the 7-arg `renderItem`'s `ItemRenderer.renderItem` and displaces the item on the PoseStack only) and third-person magic (the same feature calls `renderItem` twice more through a `MagicGlow` wrapper provider; last-write-wins stores the glow copy). Its `MixinWorldRenderer` is priority 0 at `render`'s `drawCurrentLayer()`, not `renderEntities`. |
| 3D Skin Layers | 3D outer skin layer on the first-person arm | U: shouldn't move the item. 1.21.11 (1.11.3, checked 2026-09-22): compatible |
| Entity Model Features / Entity Texture Features, Fresh Animations (pack) | entity models and textures | U: the bobber and third-person players; the first-person hand is not expected to change. 1.21.11 (EMF 3.3.8, checked 2026-09-22): compatible |

### Fishing mods and modded rods

| Mod | What it changes that matters | Status |
|---|---|---|
| Tide | its own hook renderer | L. no 1.21.11 build (checked 2026-09-22) 1.21.10: no build (checked 2026-09-22). 1.21.8: no build (checked 2026-09-22; `Tide-2` registers its own hook renderer for its own entity, so there would be no load conflict either). 1.21.4 (Tide 2 1.6.1, `Lightning-64/Tide` branch `1.21.4`, checked 2026-09-23): L, and **no load conflict** — no mixin on `FishingBobberEntityRenderer` or `FishingBobberEntity`; it registers `TideFishingHook extends Projectile` with its own `TideFishingHookRenderer` carrying a copy of vanilla's hand-position maths, and its `ItemsMixin` HEAD-cancels vanilla registration of `minecraft:fishing_rod` (`TideItems.ITEM_OVERRIDE_MAP`), so with Tide installed vanilla hooks are essentially never cast and the fix is inert. |
| Enchanted Fishing Line (`enchanted_fishing_line`) | reads the line origin for its rope simulation, and ships its own copy of an older version of this fix as a cancellable `@Inject(RETURN)` on the hand-position method: with both loaded the corrections stack | **Declared incompatible**: `"breaks": { "enchanted_fishing_line": "*" }` in `fabric.mod.json` on every branch, no EFL-specific code (user decision 2026-09-22); check the entry is there. Why (26.2 1.0.0, checked 2026-09-22; builds also exist for 26.1–26.1.2 and 1.21.11, none for 26.3): the RETURN inject is a verbatim copy of v0.5's correction with no `fishingrodfix` check. With v0.5 both RETURN injects cancel and the first wins (one correction); with v0.6's first-person MEV it applies on top (off the rod away from 16:9, with FOV modifiers, turning, crouching). Its rope: `lambda$submit$1` HEAD cancel plus its own `submitCustomGeometry` at `submit` TAIL reading `lineOriginOffset`, so the body-held offset and the line hiding don't reach it 1.21.10 (checked 2026-09-22): no 1.21.9-1.21.10 build; the `breaks` entry is in `fabric.mod.json` anyway. 1.21.8: no build (Fabric builds jump 1.21.1 → 1.21.11, checked 2026-09-22); the `breaks` entry is present anyway. 1.21.5: **no build** (checked 2026-09-23); the `breaks` entry is present in `fabric.mod.json` anyway. 1.21.4: no build (Modrinth `game_versions` = 1.20.1, 1.21.1, 1.21.11, 26.1, 26.1.1, 26.1.2, 26.2; checked 2026-09-23); the `breaks` entry is present in `fabric.mod.json` anyway. |
| Better Fishing (`better_fishing`) | doesn't touch the origin | H (observed 2026-09-21) |
| Polytone | line offsets | L. 26.1 (checked 2026-09-22): `stringVertex` HEAD argument change (the L) and a colour `@ModifyArg`; its `SubmitNodeStorageMixin` targets only `submitModel` HEAD. 1.21.11 (6.6.0, checked 2026-09-22): the L as above; its queue mixin targets `submitModel`, not `clear()` 1.21.10 (3.6.3, branch `1.21.10`, checked 2026-09-22): L: a `renderFishingLine` HEAD argument change and a colour `@ModifyArg`; its queue mixin doesn't touch `clear()`. 1.21.8 (1.21.8-3.5.4-alpha, branch `1.21.6`, checked 2026-09-22): H — **no** fishing-line mixin on this version; its `GameRendererMixin` (priority 500) injects at `render`'s `new GuiGraphics` and TAIL, not at `GlobalSettings.set`; its `SpriteContentsMixin` only MRVs `createAnimatedTexture`. 1.21.5 (1.21.5-3.6.0-fabric, branch `1.21.5`, checked 2026-09-23): L — `FishingHookRendererMixin` injects *inside* `stringVertex` (= `renderFishingLine`) and `@ModifyArg`s its colour; the port's `@WrapWithCondition` is on the call site, so there is no conflict (a hidden line simply never calls it), and its line offsets remain the documented L. Its `GameRendererMixin` (priority 500) injects at `render`'s `new GuiGraphics` and TAIL, not at `isFinishedLoading`; `LevelRendererMixin` (1300) touches only `renderLevel` clouds/fog; `SpriteContentsMixin` MRVs `createAnimatedTexture`, which stays consistent with `getFrameCount()`. 1.21.4 (`1.21.4-3.3.23`/`3.3.24`, branch `1.21.4`, checked 2026-09-23): H — **no fishing-line mixin on this version** (`FishingHookRendererMixin` first appears on the 1.21.5 branch), so neither its colour change nor its line offsets apply. Its `SpriteContentsMixin` only MRVs `createAnimatedTexture` (consistent with the port's `getFrameCount()` stand-in), its `GameRendererMixin` (priority 500) injects at `render`'s `new GuiGraphics` and TAIL (not at `isFinishedLoading`), and its `LevelRendererMixin` (1300) touches only `renderWorld` clouds/fog. Its only line renderer is `LeashMixin` (`EntityRenderer`, priority 1300). |
| Rods from other mods | a `FishingRodItem` subclass counts as a rod; anything else casts hooks that keep vanilla's line and are never hidden | H. L: a modded rod with its own texture or model isn't measured (only `item/fishing_rod_cast` is) |
| Woodwalkers (`walkers`) | morphs the player's render entirely | L: vanilla's value. 1.21.4 (1.21.4-fabric-6.3.6, `ToCraft/woodwalkers-mod` branch `v6.3`, checked 2026-09-23): `PlayerEntityRendererMixin` (priority 1001) overrides `PlayerEntityRenderer.render` and returns early for a morphed player, so `LivingEntityRenderer.render` and every feature layer — including `HeldItemFeatureRenderer` — never run. No fishing, `GameRenderer`, `WorldRenderer` or `HeldItemRenderer` mixin. |

### Resource packs

| Pack | What it changes that matters | Status |
|---|---|---|
| Vanilla | the calibration itself | H |
| Programmer Art | older rod texture | U: check its cast-rod tip against vanilla's |
| Faithful 32x | rod tip at (14.75, 1.75)/16 | H: measured tip shift |
| Faithful 64x, Default HD 128x, other texture-only redraws | redrawn rod, higher resolution | H by design while the tip moves less than `MAX_TIP_SHIFT`. U per pack; high resolutions: the measurement's one-off cost (`performance.md`) |
| Animated cast-rod textures | several frames | H: unique frames are unioned |
| 3D rod models; packs that change `handheld_rod`'s display transform; per-item model swaps (CIT Resewn, Polytone, server `custom_model_data` / `item_model`) | a different model or pose | L |
| Popular packs that don't touch the rod (Low on Fire, Low Shield, …) | — | no check needed |

### Shader packs (through Iris)

Complementary Reimagined / Unbound, BSL, Bliss, Mellow, Photon, MakeUp and the rest share Iris'
pipeline, so the Iris row applies to all of them. Pack-specific:

| Behavior | Status |
|---|---|
| hand sway or other hand transforms in the pack's hand program | L |
| TAA jitter of the projection | U: jitter should only touch off-diagonal terms, not the `m11` the world FOV is read from; verify Moot on the branches where the world FOV comes from `GameRenderer.getFov` rather than from a projection matrix (1.21.5, 1.21.6–1.21.8, 1.21.11): jitter can't reach it. |
| hand FOV or "hand depth" changed in the pack | U |

### Servers and gameplay

| Situation | Status |
|---|---|
| high ping, `/tick freeze` | H: line hiding, the remembered rod hand |
| a cast, then a slot switch before the hook reaches the client, on a ping longer than the drawn items' lag | L |
| server custom rods (`custom_model_data`, `item_model`) | L: the model isn't measured. H: hooks cast by non-rod items keep vanilla's line |
| other players' hooks | H: line hiding; the first-person origin is only for the local player's own view; where the branch corrects the third-person origin, it applies to every player's body |

### Loaders and platforms

| Loader | Status |
|---|---|
| Fabric Loader | the declared floor must ship every Mixin / MixinExtras feature used (`correctness.md`) Loader 0.15.10 (the declared floor on 1.21.5 and 1.21.6–1.21.8) bundles MixinExtras **0.3.5**, which already ships `injector/v2/WrapWithCondition` and `@Local(argsOnly)` — the floor covers everything the mod uses (checked 2026-09-23). |
| Quilt | U: check nothing loader-internal is used beyond `FabricLoader` |
| Sinytra Connector (Fabric mods on NeoForge) | U |

## D. Procedure

1. **Touch points.** From the scope, list the vanilla targets (injectors), the vanilla state and
   methods read or invoked, and the behaviors that changed (what now falls back, is newly corrected,
   or is hidden).
2. **Matrix pass.** For every row in section C whose "what it changes" overlaps a touch point (and
   every input in section B the change reads), re-derive the outcome under the change: crash or
   conflict at load · wrong line (worse than vanilla) · vanilla's line (fallback) · corrected ·
   unaffected. Compare with the row's status: an H row must still hold; an L row's text must still
   be accurate; a U row the change makes relevant gets a verdict with evidence, or stays U with what
   would settle it.
3. **Discovery** — only for touch points sections B and C don't cover yet (a new injector target, a
   vanilla method or piece of state the mod didn't read or invoke before), or when the scope is
   `all`. Otherwise skip it and say so: the matrix already stands for those targets. Find the mods
   the matrix doesn't know that touch the new points:
   - Modrinth, by downloads, for this branch's MC version:
     ```sh
     curl -s -G https://api.modrinth.com/v2/search \
       --data-urlencode 'query=<term>' \
       --data-urlencode 'facets=[["versions:<mc>"],["categories:fabric"],["project_type:mod"]]' \
       --data-urlencode 'index=downloads' --data-urlencode 'limit=20'
     ```
     Terms: fishing, rod, zoom, fov, camera, bobbing, hand, first person, viewmodel, animation,
     nausea, freecam. For packs use `project_type:shader` or `project_type:resourcepack` (fishing
     rod, 3d, items, hd).
   - Mixin sources on GitHub that target the same members (search the vanilla method or class name
     together with `@Mixin`); read the source where the project is open.
   - Closed-source candidates with many downloads: list them under NEEDS JAR.

   **Researching a mod** (from discovery, or a row whose hooks section C doesn't record yet) — keep
   it cheap; each step can end the research:
   - *Which build?* One request:
     `curl -s -G "https://api.modrinth.com/v2/project/<slug>/version" --data-urlencode 'game_versions=["<mc>"]' --data-urlencode 'loaders=["fabric"]'`.
     If it's empty, drop `game_versions` and take the Fabric build for the nearest MC version: the
     fix is ported to other versions and the mod may catch up, so its hooks still count (say "no
     build for <mc> yet, checked <version>"). Skip a mod only if it has no Fabric build for any MC
     version this mod ships for (its version branches). A mod that is only on CurseForge or GitHub:
     check its releases there.
   - *What does it hook?* Only the mod's mixin config(s) (named in its `fabric.mod.json`), on the
     branch or tag of that build, and then only the mixin classes that target the same vanilla
     classes as the scope. Don't read the rest of the repo.
   - *Already known?* A section C row that records the mod's hooks for this MC version counts as
     researched: don't research it again unless the scope touches something the row doesn't cover.
4. **Beyond the review** — list these as open items, don't do them (they need the user's OK):
   - *Jar inspection* (NEEDS JAR): download the jar, then read its `fabric.mod.json` (mod id, mixin
     configs), the mixin configs, and `javap -c -p` the mixins that target the same classes:
     injector type, target, priority, `require`, cancels. Say what you would check in it.
   - *Load test*, when loading together is the open question: the smoke-test client
     (`port-version` Step 7, scratch game dir) with the other mod and its dependencies in
     `<game dir>/mods/`. Pass: a `Mixing` line for each of our mixins and no mixin error or conflict
     in the log. It runs third-party code and proves loading only.
5. **Outcomes.** A crash or conflict: REAL BUG. A regressed H row: REAL BUG. A new worse-than-vanilla
   line: REAL BUG unless the user accepts it as a Known limitation. A limitation that changed without
   `CLAUDE.md` changing: WRONG FACT. A matrix row found wrong: propose the edit to this file
   (IMPROVEMENT).

## Report format

1. **Touch points**: targets, inputs read, behaviors changed.
2. **Rows checked**: each matrix row or input you evaluated, its outcome under the change, and the
   evidence (code line, the other mod's source); rows you left U, with what would settle them.
3. **Discovered**: mods found in step 3, with what they hook.
4. **Findings**, each:

```text
### [REAL BUG | WRONG FACT | IMPROVEMENT | NIT] <one-line title>
- Where: <file:line>; other side: <mod / pack, version, its source file:line or mixin>
- Scenario: <which mods, packs or settings> → <crash, conflict, wrong line, doc mismatch>
- Evidence: <the lines that prove it; quote briefly>
- Suggested fix: <what to change, or the limitation text to add>
- Confidence: high | medium | low
```

5. **NEEDS JAR**: name, Modrinth URL, size, and what you would check in it.
6. **Matrix updates**: for every mod you researched, a ready-to-paste section C row: the mod (its
   id), the MC version and mod version checked, what it hooks (class, method, injector; whether it
   redirects, overwrites or cancels), the source link, and the status. Once added to this file, the
   next review doesn't research that mod again.
